version: '3'

services:
  auth-api-service:
    image: adxl/bng-api-auth:latest
    environment:
      STAGE: production
      DATABASE_URL: '@DATABASE_URL@'
    networks:
      - bng
    deploy:
      replicas: 3

networks:
  bng:
    external: true
