version: '3'

services:
  api:
    image: adxl/bng-api-auth:latest
    build:
      context: .
      target: production

  db:
    image: adxl/bng-db-auth:latest
    build:
      context: .
      dockerfile: Dockerfile.db
